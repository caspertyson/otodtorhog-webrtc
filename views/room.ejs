<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>VIDEO CHAAAAAAT</title>
    <script>
        const ROOM_ID = "<%= roomId %>"
    </script>
    <script defer src="https://unpkg.com/peerjs@1.3.1/dist/peerjs.min.js"></script>
    <script src="/socket.io/socket.io.js" defer></script>
    <script src="script.js" defer></script>

    <style>
        body{
            min-width: 1537px;
        }
        #video-grid{
            display: grid;
            grid-template-columns: repeat(4, 240px);
            grid-auto-rows: 240px;
            gap: 5px;
        }
        video{
            height: 100%;
            width: 100%;
            object-fit: cover;
            margin: 4px;
            border-radius: 5px;
        }
        button{
            margin-top: 5px;
            margin-left: 5px;
            width: 240px;
        }
        #button-container{
            margin-left: 244px;
            height: 30px;
        }
        input{
            width: 292px;
        }
        #score-container{
            margin-left: 4px;
        }
        #start-button{
            background-color: red;
            width: 50px;
            height: 50px;
            margin-top: 30px;
        }
        #user-container{
            display: flex;
        }
        #score-board{
            margin-left: 20px;
        }
        p{
            margin: 2px;
        }
        #leader-board-title{
            margin-bottom: 7px;
        }
    </style>
</head>
<body>
    <div id="user-container">
            <span id="video-grid"></span>
            <span id="score-board">
                <div id="leader-board-title">LEADER BOARD   </div>
            </span>
    </div>
    <div id="button-container"></div>
    <div id="score-container">
        <button id="start-button">Start Button</button>

    </div>
</body>
</html>