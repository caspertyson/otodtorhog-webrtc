<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>VIDEO CHAT</title>
    <script>
        const ROOM_ID = "<%= roomId %>"
    </script>
    <script defer src="https://unpkg.com/peerjs@1.3.1/dist/peerjs.min.js"></script>
    <script src="/socket.io/socket.io.js" defer></script>
    <script src="script.js" defer></script>
    <style>
        body{
            min-width: 1537px;
            background-color: rgba(14,190,213,255);
        }
        canvas{
            background-color: rgba(220, 9, 16, .88);
        }
        #video-grid{
            display: grid;
            grid-auto-flow: column;
            gap: 5px;

        }
        video{
            height: 180px;
            width: 180px;
            object-fit: cover;
            margin: 4px;
            border-radius: 5px;
        }
        button{
            margin-top: 5px;
            margin-left: 10px;
            width: 180px;
            height: 40px;
            border-radius: 5px;
        }
        #button-container{
            margin-left: 188px;
            height: 50px;
        }
        input{
            width: 292px;
        }
        #score-container{
            margin: 4px;
        }
        #start-button{
            background-color: red;
            color: black;
            width: 200px;
            height: 50px;
            margin-top: 30px;
        }
        #user-container{
            display: flex;
        }
        #score-board{
            margin-left: 20px;
        }
        p{
            margin: 2px;
        }
        #leader-board-title{
            margin-bottom: 7px;
        }

        .player{
            padding: 5px;
            border-radius: 4px;
            border-left: 3px solid red;
            background-color: #F08080;
        }
        #leaderboardTitle{
            color: black;
            padding: 3px;
            border-radius: 4px;
        }
        #next_question{
            background-color: red;
        }
        #mute{
            background: url("/unmute.png") no-repeat;
            background-position: center;
            width: 50px;
            height: 50px;
            margin-top: 50px;
        }
    </style>
</head>
<body id="body">
    <p id="name-user"></p>
    <div id="user-container">
            <span id="video-grid"></span>
            <span id="score-board">
                <div id="leader-board-title"></div>
            </span>
    </div>
    <div id="button-container"></div>
    <div id="score-container">
        <button id="start-button">Waiting For Players</button>
        <button id="mute"></button>
    </div>

</body>
</html>